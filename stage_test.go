package main

import "testing"

func testStageElectricJellyfish(t *testing.T) {
	t.Skip("TODO implement test")

	t.Run("stage 1 begins once wes has eaten enough jellyfish", func(t *testing.T) {
	})

	t.Run("a random group of jellyfish is set to attack on a recurring timer", func(t *testing.T) {
	})

	t.Run("stage effects remain active in later stages", func(t *testing.T) {
	})
}
